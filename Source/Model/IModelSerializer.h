////////////////////////////////////////////////////////////////////////////////
//
/// Model/IModelSerializer.h содержит интерфейс объекта, сериализующего модель
/**
*/
////////////////////////////////////////////////////////////////////////////////
#pragma once

#include <istream>
#include <memory>

struct IModel;

////////////////////////////////////////////////////////////////////////
//
/// Интерфейс контроллера для предоставления доступа к данным модели.
/**
*/
////////////////////////////////////////////////////////////////////////
struct IModelSerializer
{
  virtual bool serialize(const IModel & model, std::ostream & out) = 0;
  virtual std::unique_ptr<IModel> deSerialize(std::istream & in) = 0;
};
